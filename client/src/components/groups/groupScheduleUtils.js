/**
 * @deprecated Import from `utils/calendarDates` instead. This module is now a
 * thin compatibility shim that re-exports the canonical calendar utilities so
 * existing import sites keep working; it will be removed once they migrate.
 */
export * from '../../utils/calendarDates';
